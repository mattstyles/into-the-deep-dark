{
  "name": "@itdd/configApp",
  "private": true,
  "description": "Config used during build time",
  "version": "0.1.0",
  "main": "lib/itdd-config-app.js",
  "module": "lib/itdd-config-app.esm.js",
  "scripts": {
    "test": "echo 'Nope, do it' && exit 1",
    "build": "NODE_ENV=production rk build -t core",
    "watch": "rk watch -t core"
  },
  "license": "MIT",
  "devDependencies": {
    "standard": "^14.3.4"
  },
  "dependencies": {
    "@raid/addons": "^6.0.0"
  }
}
